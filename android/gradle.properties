org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Google Maps (replace with your Android API key from Google Cloud Console)
# Get it: APIs & Services → Credentials → Create API key → restrict to "Maps SDK for Android"
MAPS_API_KEY=
